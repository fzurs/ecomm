services:
  # db:
  #   image: postgres:17
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   environment:
  #     - POSTGRES_DB=ecommdb
  #     - POSTGRES_USER=admin
  #     - POSTGRES_PASSWORD=admin
  api:
    build:
      context: ./apps/api
    container_name: ecomm_api
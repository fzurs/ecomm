{
  "name": "ecomm",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "generate:schema": "pnpm --filter api run generate:schema"
  },
  "packageManager": "pnpm@10.13.1",
  "devDependencies": {
    "turbo": "^2.5.8"
  }
}
